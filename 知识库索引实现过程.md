# 知识库索引实现过程

## 索引的作用

索引是把知识库分片写入向量库的过程。

系统先把每个知识库分片拼成适合检索的文本，再调用向量模型生成 embedding，最后写入 Chroma。AI 问答时，系统会把用户问题也转成向量，再到 Chroma 中检索相似分片。

## 前置条件

| 条件 | 说明 |
| --- | --- |
| 知识库已解析 | 文档内容已经通过手动解析或 MinerU 解析写入正文 |
| 知识库已分片 | `chunkStatus` 必须是 `success` |
| 向量化配置可用 | 已配置 Chroma 地址、Collection、向量模型账号、模型和维度 |
| 向量服务可用 | 本地 Chroma 服务需要保持运行 |
| 向量维度一致 | 向量模型输出维度必须和 Chroma Collection 维度一致 |

## 处理流程

```text
点击知识库列表“索引”
  ↓
校验当前知识库是否已完成分片
  ↓
进入任务队列
  ↓
读取当前知识库下全部分片
  ↓
读取分片所属文档信息
  ↓
为每个分片拼接索引文本
  ↓
计算索引文本 contentHash
  ↓
判断哪些分片需要重新索引
  ↓
调用向量模型生成 embedding
  ↓
写入 Chroma Collection
  ↓
回写分片 vectorId、contentHash、vectorStatus、vectorizedAt
  ↓
更新知识库索引状态
```

## 索引文本结构

每个分片不会只把正文写入向量库，而是会拼接一些辅助信息。

```text
标题：分片标题
知识库：知识库名称
来源：原文件名或来源名称
命中关键字：关键词
口语化描述：用户可能的说法
匹配优先级：优先级数值
正文：
分片正文内容
```

这样做的目的是让向量检索不只依赖正文，还能利用标题、知识库名称、关键词、口语化描述等信息提高命中率。

## 写入 Chroma 的数据结构

```json
{
  "id": "kb:1:doc:2:chunk:3",
  "embedding": [0.12, 0.34, 0.56],
  "document": "标题：价保规则\n知识库：京东售后政策\n正文：...",
  "metadata": {
    "knowledgeBaseId": 1,
    "knowledgeBaseName": "京东售后政策",
    "documentId": 2,
    "chunkId": 3,
    "chunkIndex": 0,
    "title": "价保规则",
    "sourceName": "京东售后政策.pdf",
    "contentType": "pdf",
    "fileName": "京东售后政策.pdf",
    "fileUrl": "https://example.com/uploads/demo.pdf",
    "hitKeywords": "价保,退款",
    "colloquialDescription": "怎么申请价保",
    "matchPriority": 0
  }
}
```

## 增量索引机制

系统不会每次都把所有分片重新写入向量库。

每个分片索引前会生成：

```text
contentHash = sha256(索引文本)
vectorId = kb:{knowledgeBaseId}:doc:{documentId}:chunk:{chunkId}
```

如果满足以下条件，分片会跳过索引：

```text
vectorStatus = success
contentHash 未变化
vectorId 未变化
```

如果正文、标题、关键词、口语化描述、匹配优先级等影响索引文本的内容发生变化，`contentHash` 会改变，该分片会重新索引。

## 状态变化

| 阶段 | 状态 |
| --- | --- |
| 开始索引 | `indexStatus = processing` |
| 分片写入中 | `vectorStatus = processing` |
| 分片写入成功 | `vectorStatus = success` |
| 分片写入失败 | `vectorStatus = failed` |
| 知识库索引完成 | `indexStatus = success` |
| 知识库索引失败 | `indexStatus = failed` |

## 失败处理

如果索引过程中失败：

- 当前处理中的分片会标记为 `failed`
- 错误信息写入 `vectorError`
- 知识库处理结果会记录失败原因

常见失败原因：

```text
未完成分片
没有可索引的分片
向量模型未配置
向量模型调用失败
Chroma 服务未启动
Chroma Collection 维度不一致
```

