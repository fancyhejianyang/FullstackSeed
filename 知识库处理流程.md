# 知识库处理流程

本文档用于说明当前系统中「知识库文档」从创建、上传、解析、分片、索引到问答检索的完整处理顺序。目标是把流程拆清楚，后续接入向量库、OCR、Agent 或更多第三方解析服务时，可以按阶段扩展，而不是把逻辑混在一个接口里。

## 1. 核心对象

### 1.1 知识库分类

表：`knowledge_base_categories`

用途：

- 作为知识库的上级归类。
- 分类独立存在，不依赖某个知识库。
- 新建知识库时必须先选择分类。

关键字段：

| 字段 | 说明 |
| --- | --- |
| `id` | 分类 ID |
| `parentId` | 父级分类 ID |
| `name` | 分类名称 |
| `code` | 分类编码 |
| `description` | 描述 |
| `sort` | 排序 |

### 1.2 知识库主记录

表：`knowledge_bases`

用途：

- 管理一个业务知识条目或知识文档入口。
- 保存分类、名称、命中关键字、口语化描述、匹配优先级。
- 保存上传文件地址或文本内容。
- 保存整条知识库处理状态。

关键字段：

| 字段 | 说明 |
| --- | --- |
| `categoryId` | 所属分类 |
| `name` | 知识库名称 |
| `hitKeywords` | 命中关键字，用于提高检索命中 |
| `colloquialDescription` | 用户口语化问法、简称、别名 |
| `matchPriority` | 匹配优先级，分数相同时优先级越高越靠前 |
| `contentType` | 内容类型：`text`、`pdf`、`word`、`image` |
| `contentText` | 文本内容或解析后的正文缓存 |
| `fileName` | 原文件名 |
| `fileUrl` | 文件访问地址，业务数据只保存 URL |
| `processStage` | 总处理阶段 |
| `parseStatus` | 解析状态 |
| `chunkStatus` | 分片状态 |
| `indexStatus` | 索引状态 |
| `lastProcessMessage` | 最近处理结果或失败原因 |
| `isEnabled` | 是否启用，检索时只使用启用数据 |

### 1.3 知识库文档

表：`knowledge_base_documents`

用途：

- 保存一次解析后的正文。
- 作为分片的直接父级。
- 手动分片时从该表读取正文并覆盖生成分片。

关键字段：

| 字段 | 说明 |
| --- | --- |
| `knowledgeBaseId` | 所属知识库 |
| `categoryId` | 所属分类，默认继承知识库分类 |
| `title` | 文档标题 |
| `sourceType` | 来源类型：`text`、`manual`、`mineru`、`pdf`、`word`、`image` 等 |
| `sourceName` | 来源名称，一般为文件名 |
| `content` | 解析后的正文 |
| `status` | 文档状态：`draft`、`processing`、`parsed`、`failed` |
| `description` | 文档处理说明或失败原因 |
| `hitKeywords` | 文档级命中关键字 |
| `colloquialDescription` | 文档级口语化描述 |
| `matchPriority` | 文档级匹配优先级 |

### 1.4 知识库分片

表：`knowledge_base_chunks`

用途：

- 保存最终参与检索的文本片段。
- 当前问答检索优先使用分片；没有分片时才回退到文档正文或知识库正文。

关键字段：

| 字段 | 说明 |
| --- | --- |
| `knowledgeBaseId` | 所属知识库 |
| `categoryId` | 所属分类 |
| `documentId` | 所属文档 |
| `chunkIndex` | 分片序号 |
| `title` | 分片标题 |
| `content` | 分片正文 |
| `tokenCount` | 当前实际保存为字符数 |
| `sort` | 排序 |

## 2. 总体处理顺序

知识库文档处理按以下顺序执行：

1. 建分类
2. 建知识库
3. 上传文件或录入文本
4. 解析
5. 确认解析正文
6. 分片
7. 确认分片
8. 索引
9. 配置检索策略
10. 聊天应用绑定 AI 配置和检索配置
11. 问答接口按检索结果组装上下文

可以理解为：

```text
Category
  -> KnowledgeBase
      -> Document
          -> Chunk
              -> Index / Retrieval
                  -> AI Chat
```

## 3. 阶段一：创建知识库

### 3.1 前端入口

页面：

- `web/src/views/knowledge-base/Index.vue`
- `web/src/views/knowledge-base/Edit.vue`

操作：

- 点击「新增知识库」
- 选择所属分类
- 填写名称
- 填写命中关键字、口语化描述、匹配优先级
- 选择内容类型
- 文本类型直接填写正文
- 文件类型先通过统一上传组件上传文件，再保存文件 URL

### 3.2 后端接口

```http
POST /api/knowledge-bases
```

对应方法：

- `KnowledgeBasesController.createBase`
- `KnowledgeBasesService.createBase`

### 3.3 创建时状态

创建时会根据内容类型和内容是否存在初始化状态：

| 内容情况 | `processStage` | 说明 |
| --- | --- | --- |
| 文本且有正文 | `ready` | 可以进入解析 |
| 文本但无正文 | `draft` | 草稿，需补充内容 |
| 文件且有 `fileUrl` | `uploaded` | 文件已上传，可以解析 |
| 文件但无 `fileUrl` | `draft` | 草稿，需先上传文件 |

默认状态：

```text
parseStatus = pending
chunkStatus = pending
indexStatus = pending
lastProcessMessage = null
```

## 4. 阶段二：上传文件

### 4.1 上传规则

文件上传应走统一上传接口，业务表只保存可直接访问的 URL：

```text
业务数据存 fileUrl，不存 blob/base64
```

原因：

- 本地存储、服务器存储、OSS、CDN 后续都由上传服务和存储配置决定。
- 解析服务只需要通过 `fileUrl` 获取文件内容。
- MinerU 等第三方解析服务要求 URL 能被外网访问。

### 4.2 文件读取

后端手动解析读取文件时走：

- `DocumentParsersService.prepareFileContext`
- `StoredFilesService.read`

读取逻辑应区分：

- 本地服务端文件 URL
- OSS/CDN 远程 URL

## 5. 阶段三：解析

解析是把原始文本、PDF、Word、图片等内容转成统一正文。

### 5.1 知识库级解析

接口：

```http
POST /api/knowledge-bases/:id/parse
```

请求参数：

```json
{
  "parseMode": "manual"
}
```

或：

```json
{
  "parseMode": "mineru"
}
```

处理方法：

- `KnowledgeBasesService.parseBase`
- `KnowledgeBasesService.executeParseBase`
- `KnowledgeBasesService.parseBaseContentByMode`

### 5.2 文档级解析

接口：

```http
POST /api/knowledge-bases/documents/:id/parse
```

处理方法：

- `KnowledgeBasesService.parseDocument`
- `KnowledgeBasesService.executeParseDocument`
- `KnowledgeBasesService.parseDocumentByMode`

文档级解析更适合单独文档操作；知识库级解析更适合列表页按知识库主记录处理。

### 5.3 解析模式

当前支持两种解析模式：

| 模式 | 值 | 说明 |
| --- | --- | --- |
| 手动解析 | `manual` | 使用后端内置解析器 |
| MinerU 解析 | `mineru` | 调用 MinerU 配置服务 |

如果没有传 `parseMode`，默认走：

```text
manual
```

### 5.4 手动解析

入口：

- `DocumentParsersService.parse`

解析器：

| 内容类型 | 解析器 | 当前说明 |
| --- | --- | --- |
| `text` | `TextDocumentParser` | 直接读取文本 |
| `pdf` | `PdfDocumentParser` | 普通 PDF 解析，扫描件需 OCR 或 MinerU |
| `word` | `WordDocumentParser` | 当前支持 `.docx`，并把 Word 链接转成括号内联 |
| `image` | `ImageDocumentParser` | OCR 能力预留或依赖 AI/OCR 配置 |

手动解析成功后：

1. 文档 `content` 写入解析正文
2. 文档 `status = parsed`
3. 文档 `sourceType = manual`
4. 同步知识库 `contentText`
5. 清理旧分片
6. 触发自动分片

### 5.5 MinerU 解析

入口：

- `MineruConfigsService.createParseTask`
- `MineruConfigsService.waitForSuccess`

处理顺序：

1. 检查是否存在可用 MinerU 配置
2. 用文件 URL 创建 MinerU 解析任务
3. 保存任务 ID 到文档描述
4. 按配置轮询任务结果
5. 成功后读取 Markdown
6. 写入文档 `content`
7. 同步知识库 `contentText`
8. 清理旧分片
9. 触发自动分片

注意：

- MinerU 需要可公网访问的文件 URL。
- 如果 URL 是本地 `localhost` 或 OSS 私有读，第三方服务可能无法读取。
- 文件如果在 OSS 中，需要确认 ACL、公网域名、签名 URL 或 CDN 访问策略。

### 5.6 解析状态变化

知识库主记录状态：

| 时机 | `processStage` | `parseStatus` | `chunkStatus` | `indexStatus` | `lastProcessMessage` |
| --- | --- | --- | --- | --- | --- |
| 提交解析任务 | `parsing` | `processing` | `pending` | `pending` | 解析任务已提交 |
| 执行解析中 | `parsing` | `processing` | `pending` | `pending` | 正在解析内容 |
| 解析成功 | `parsed` | `success` | `pending` | `pending` | 解析完成，等待分片 |
| 解析失败 | `failed` | `failed` | 不变 | 不变 | 错误原因 |

## 6. 阶段四：保存解析正文

解析完成后会生成或更新一条知识库文档：

方法：

- `KnowledgeBasesService.saveBaseDocument`
- `KnowledgeBasesService.applyThirdPartyMarkdown`
- `KnowledgeBasesService.syncBaseParsedContent`

写入规则：

| 数据 | 写入位置 |
| --- | --- |
| 解析正文 | `knowledge_base_documents.content` |
| 正文缓存 | `knowledge_bases.contentText` |
| 来源类型 | `knowledge_base_documents.sourceType` |
| 来源名称 | `knowledge_base_documents.sourceName` |
| 关键字 | 优先继承知识库主记录 |
| 口语化描述 | 优先继承知识库主记录 |
| 匹配优先级 | 优先继承知识库主记录 |

## 7. 阶段五：分片

分片是把解析正文切成多个较小文本段，供后续检索和问答使用。

### 7.1 自动分片

接口：

```http
POST /api/knowledge-bases/:id/chunk
```

处理方法：

- `KnowledgeBasesService.chunkBase`
- `KnowledgeBasesService.executeChunkBase`
- `KnowledgeBasesService.saveDocumentChunks`
- `KnowledgeBasesService.splitMarkdown`

前置条件：

```text
parseStatus = success
```

流程：

1. 读取默认分片配置
2. 找到该知识库最近一次解析生成的文档
3. 检查文档正文
4. 删除该文档旧分片
5. 按配置分片
6. 写入 `knowledge_base_chunks`
7. 更新知识库状态为已分片

### 7.2 分片配置

表：`knowledge_chunk_configs`

配置项：

| 字段 | 说明 |
| --- | --- |
| `chunkSize` | 分片大小，默认 1200 字符 |
| `chunkOverlap` | 分片重叠，默认 120 字符 |
| `separator` | 切分方式：`length` 或 `paragraph` |
| `preserveHeading` | 是否保留标题，当前为配置字段 |
| `timeoutMinutes` | 自动分片超时时间 |
| `isDefault` | 是否默认配置 |
| `isEnabled` | 是否启用 |

### 7.3 自动分片方式

当前支持：

| 方式 | 说明 |
| --- | --- |
| 定长切分 | 按 `chunkSize` 截取，按 `chunkOverlap` 回退重叠 |
| 段落切分 | 优先按空行分段，段落过长再回退定长切分 |

分片时会检查超时：

```text
如果超过 timeoutMinutes，则抛出错误：
自动分片超时，请调大超时时间或改用手动分片
```

### 7.4 手动分片

接口：

```http
POST /api/knowledge-bases/documents/:id/chunks/manual
```

处理方法：

- `KnowledgeBasesService.replaceDocumentChunks`

前端入口：

- 知识库详情页
- 内容 Tab
- 点击「手动分片」

前端当前能力：

- 使用 PixiJS 渲染解析正文
- 鼠标按住拖动选择文本区域
- 生成分片草稿
- 右侧展示已分好的片段
- 删除某个分片后释放对应原文区域
- 保存后覆盖该文档已有分片

保存后：

1. 删除该文档旧分片
2. 按用户手动片段写入新分片
3. 文档描述写入：`手动分片完成，共 N 个分片`
4. 知识库主记录更新：

```text
processStage = chunked
chunkStatus = success
indexStatus = pending
lastProcessMessage = 手动分片完成，共 N 个分片
```

## 8. 阶段六：索引

接口：

```http
POST /api/knowledge-bases/:id/index
```

处理方法：

- `KnowledgeBasesService.indexBase`
- `KnowledgeBasesService.executeIndexBase`

前置条件：

```text
chunkStatus = success
```

当前逻辑：

1. 检查是否已有分片
2. 更新索引状态为处理中
3. 标记索引完成

当前说明：

```text
索引标记已完成，向量数据库写入逻辑已预留
```

也就是说，当前索引阶段还没有真正写入向量数据库；后续接 Chroma 或其它向量库时，应在这个阶段扩展。

建议后续索引顺序：

1. 读取分片
2. 读取向量模型 AI 功能配置
3. 调用 embedding 接口生成向量
4. 写入向量数据库
5. 保存向量库 collection / document id 映射
6. 更新 `indexStatus = success`

## 9. 阶段七：检索配置

表：`knowledge_retrieval_configs`

用途：

- 定义 AI 问答时从哪些知识库或分类中检索。
- 控制检索模式、召回数量、最低分、权重等。

关键字段：

| 字段 | 说明 |
| --- | --- |
| `retrievalMode` | `fullText`、`vector`、`hybrid` |
| `categoryIds` | 知识库分类范围 |
| `knowledgeBaseIds` | 具体知识库范围 |
| `topK` | 召回上限 |
| `minScore` | 默认最低分 |
| `rrfK` | RRF 融合参数 |
| `textWeight` | 文本权重 |
| `vectorWeight` | 向量权重 |
| `enableRerank` | 是否启用重排 |
| `rerankAiFeatureConfigId` | 重排使用的 AI 功能配置 |
| `isEnabled` | 是否启用 |

当前真实检索实现：

- 先实现全文/关键词兜底检索。
- 当前问答检索优先使用 `knowledge_base_chunks`。
- 如果没有分片，则回退到 `knowledge_base_documents.content`。
- 如果没有文档正文，则回退到 `knowledge_bases.contentText`。

向量检索、混合检索、重排属于后续扩展方向。

## 10. 阶段八：AI 问答使用知识库

### 10.1 聊天应用绑定

聊天应用应绑定：

1. AI 聊天配置
2. 知识库检索配置
3. AppId
4. 白名单域名

应用端调用流式问答接口时：

- 请求头传 `appid`
- 后端按 appid 匹配聊天应用
- 后端自行读取 domain / origin 校验白名单
- 该接口不走后台 Authorization，而走 appid + domain 校验

### 10.2 检索上下文构建

服务：

- `KnowledgeAiChatRetrievalService.buildReferenceContext`

流程：

1. 读取聊天应用绑定的检索配置
2. 根据配置解析知识库范围
3. 读取启用的知识库
4. 优先读取分片作为候选
5. 计算文本分数
6. 按分数和匹配优先级排序
7. 截取 `topK`
8. 拼成参考资料上下文
9. 传给大模型问答

候选分数当前参考：

| 命中位置 | 加分 |
| --- | --- |
| 完整问题命中内容 | 高 |
| 标题命中 | 较高 |
| 命中关键字命中 | 较高 |
| 口语化描述命中 | 中 |
| 内容命中 | 基础 |
| 匹配优先级 | 额外加权 |

## 11. 任务队列

服务：

- `TaskQueueService`

当前特性：

- 内存队列
- 单进程顺序执行
- 任务状态：`pending`、`running`、`success`、`failed`
- 返回 `taskId`
- 失败时保存 `errorMessage`

当前使用场景：

| 任务名 | 说明 |
| --- | --- |
| `knowledge-base.parse` | 知识库级解析 |
| `knowledge-base.chunk` | 知识库级自动分片 |
| `knowledge-base.index` | 知识库级索引 |
| `knowledge-base-document.parse` | 文档级解析 |

注意：

- 当前任务队列是内存实现，服务重启后任务记录会丢失。
- 如果后续用于生产，应考虑持久化队列，例如 BullMQ、数据库任务表或消息队列。

## 12. 状态总览

### 12.1 `processStage`

| 状态 | 说明 |
| --- | --- |
| `draft` | 草稿，内容不完整 |
| `ready` | 文本已准备好 |
| `uploaded` | 文件已上传 |
| `parsing` | 解析中 |
| `parsed` | 已解析 |
| `chunking` | 分片中 |
| `chunked` | 已分片 |
| `indexing` | 索引中 |
| `indexed` | 已索引 |
| `failed` | 处理失败 |

### 12.2 子状态

| 字段 | 状态 | 说明 |
| --- | --- | --- |
| `parseStatus` | `pending`、`processing`、`success`、`failed` | 解析状态 |
| `chunkStatus` | `pending`、`processing`、`success`、`failed` | 分片状态 |
| `indexStatus` | `pending`、`processing`、`success`、`failed` | 索引状态 |

### 12.3 失败回写

失败信息统一写入：

```text
lastProcessMessage
```

文档级失败写入：

```text
knowledge_base_documents.description
```

前端列表的「处理结果」列读取最近处理消息，方便用户直接看到失败原因。

## 13. 推荐操作顺序

### 13.1 文本知识库

```text
新建分类
  -> 新建知识库，内容类型选择文本
  -> 输入正文
  -> 保存
  -> 解析
  -> 查看正文
  -> 自动分片或手动分片
  -> 查看分片
  -> 索引
  -> 配置检索策略
  -> 聊天应用绑定检索配置
  -> 测试问答
```

### 13.2 Word / PDF 文件知识库

```text
新建分类
  -> 新建知识库，内容类型选择 Word/PDF
  -> 上传文件
  -> 保存
  -> 选择手动解析或 MinerU 解析
  -> 查看解析正文
  -> 修正或手动分片
  -> 保存分片
  -> 索引
  -> 配置检索策略
  -> 聊天应用绑定检索配置
  -> 测试问答
```

### 13.3 扫描件 / 图片类文档

```text
上传文件
  -> 优先选择 MinerU 或 OCR 能力
  -> 解析出文本
  -> 检查 OCR 正文质量
  -> 分片
  -> 索引
  -> 测试问答
```

说明：

- 扫描件 PDF 如果普通 PDF 解析拿不到正文，应走 OCR 或 MinerU。
- 图片解析应绑定 OCR 类型 AI 功能配置或 MinerU 能力。

## 14. 前端页面与菜单建议

建议菜单结构：

```text
知识库管理
  -> 知识库分类
  -> 知识库列表
  -> 分片配置
  -> 知识库检索配置
```

列表页建议操作顺序：

```text
查看
编辑
删除
解析
分片
索引
```

详情页建议 Tab：

```text
基础信息
内容
分片内容
```

内容 Tab：

- 展示解析正文
- 提供手动分片入口
- 手动分片左右对照展示：
  - 左侧原文内容
  - 右侧分好的片段

## 15. 当前已实现与待扩展边界

### 15.1 已实现

- 知识库分类
- 知识库主记录
- 文本/文件创建
- 统一上传后保存 URL
- 手动解析入口
- MinerU 解析入口
- 解析正文保存
- 自动分片
- 手动覆盖分片
- 分片配置
- 索引状态标记
- 检索配置
- 问答前拼接知识库参考资料

### 15.2 待扩展

- 真实向量索引写入
- 向量检索
- 混合检索的真实融合
- RRF 真实融合
- Rerank 真实调用
- OCR 解析链路增强
- 任务队列持久化
- 分片与向量库 ID 映射表
- 检索日志与命中来源解释

## 16. 下一阶段执行计划：手动分片到真实向量索引

目标：

把当前已经可用的手动分片结果整理成可稳定向量化的数据，形成下面这条完整链路：

```text
分片
  -> 规范化
  -> 补充来源信息
  -> 生成稳定顺序与索引标识
  -> 标记待向量化
  -> 调用向量模型
  -> 写入向量库
  -> 回写索引状态
```

### 16.1 先修正分片保存的基础一致性

当前手动分片新增走单条接口：

```text
POST /api/knowledge-bases/chunks
```

前端已能传：

- `documentId`
- `title`
- `content`
- `coreContent`
- `manualStartOffset`
- `manualEndOffset`
- `contextBeforeLength`
- `contextAfterLength`

但下一步执行前需要先补齐以下规则：

1. 单条创建分片时，如果前端没有传 `chunkIndex` 和 `sort`，后端按当前文档已有分片数量自动生成。
2. 单条创建分片时，如果前端没有传 `tokenCount`，后端按 `content.length` 写入，当前字段先按字符数使用。
3. 单条删除分片后，后端重排同文档下剩余分片的 `chunkIndex` 和 `sort`，保证顺序连续。
4. 单条更新分片内容、标题、坐标或上下文后，将知识库 `indexStatus` 重置为 `pending`。
5. 自动分片、手动覆盖分片、单条创建分片统一走同一套分片规范化方法，避免多处逻辑不一致。

建议新增内部方法：

```text
normalizeChunkContent
buildChunkTitle
nextChunkOrder
reorderDocumentChunks
syncDocumentChunkState
markBaseIndexPendingByDocument
```

### 16.2 补充分片向量化字段

在 `knowledge_base_chunks` 上补充以下字段：

| 字段 | 类型建议 | 说明 |
| --- | --- | --- |
| `vectorId` | varchar(180), nullable | 写入向量库使用的稳定 ID |
| `contentHash` | varchar(64), nullable | 分片内容摘要，用于判断是否需要重新向量化 |
| `vectorStatus` | varchar(30), default `pending` | 分片级向量状态 |
| `vectorError` | text, nullable | 单个分片向量化失败原因 |
| `vectorizedAt` | datetime, nullable | 分片最近一次向量化完成时间 |

推荐状态：

| 状态 | 说明 |
| --- | --- |
| `pending` | 待向量化 |
| `processing` | 向量化中 |
| `success` | 已写入向量库 |
| `failed` | 向量化失败 |
| `skipped` | 内容未变化，跳过重建 |

`vectorId` 建议格式：

```text
kb:{knowledgeBaseId}:doc:{documentId}:chunk:{chunkId}
```

原因：

- `chunkId` 稳定且唯一。
- 更新同一个分片时可以覆盖旧向量。
- 删除分片时可以精确删除对应向量。

`contentHash` 建议基于以下内容生成：

```text
content + title + hitKeywords + colloquialDescription + matchPriority
```

这样标题、关键词、口语化描述发生变化时，也能触发重新向量化。

### 16.3 构建向量化文本与 metadata

向量模型不建议只吃 `content`，应在服务端构造一份更适合检索的文本：

```text
标题：{chunk.title}
知识库：{base.name}
来源：{document.sourceName}
命中关键字：{base/document.hitKeywords}
口语化描述：{base/document.colloquialDescription}
正文：
{chunk.content}
```

写入向量库 metadata 建议包含：

| 字段 | 来源 |
| --- | --- |
| `knowledgeBaseId` | chunk |
| `categoryId` | chunk |
| `documentId` | chunk |
| `chunkId` | chunk |
| `chunkIndex` | chunk |
| `sort` | chunk |
| `title` | chunk/document/base |
| `sourceName` | document |
| `contentType` | base |
| `fileName` | base |
| `fileUrl` | base |
| `hitKeywords` | document 优先，base 兜底 |
| `colloquialDescription` | document 优先，base 兜底 |
| `matchPriority` | document 优先，base 兜底 |
| `manualStartOffset` | chunk |
| `manualEndOffset` | chunk |

检索结果返回给 AI 时，优先用 `metadata` 解释来源，不要只返回纯文本。

### 16.4 实现索引任务的真实顺序

当前 `indexBase` 只是标记“索引完成，向量数据库写入逻辑已预留”。下一步应改成真实异步任务：

```text
POST /api/knowledge-bases/:id/index
  -> 校验 chunkStatus = success
  -> base.indexStatus = processing
  -> 查询该知识库下所有未删除分片
  -> 计算每个分片 contentHash
  -> contentHash 未变且 vectorStatus = success 的分片跳过
  -> 其余分片标记 processing
  -> 调用 embedding 模型生成向量
  -> 写入向量库
  -> 分片 vectorStatus = success
  -> 全部成功后 base.indexStatus = success
```

失败处理：

```text
单个分片失败
  -> chunk.vectorStatus = failed
  -> chunk.vectorError = 错误原因
  -> base.indexStatus = failed
  -> base.lastProcessMessage = 汇总错误
```

如果部分成功、部分失败，不应假装整库成功；后续可以增加“重试失败分片”的按钮。

### 16.5 向量模型选择

向量化应读取 AI 功能配置中 `embedding` 类型的配置：

```text
AI 功能配置 featureType = embedding
```

要求：

- 必须选择大模型账号。
- 必须选择该账号下的向量模型。
- 一个默认可用的 embedding 配置用于知识库索引。
- 如果没有启用的 embedding 配置，索引接口直接返回明确错误。

后续如果要做不同知识库使用不同 embedding 配置，可以再给知识库或检索配置增加绑定字段。当前阶段先使用启用的默认 embedding 配置。

### 16.6 向量库写入策略

建议先抽象一个服务，不把 Chroma、Qdrant、Milvus 等实现写死在业务 service 中：

```text
KnowledgeVectorService
  -> upsertChunks(chunks)
  -> deleteChunks(vectorIds)
  -> search(queryEmbedding, filter, topK)
```

第一版优先接入 Chroma。业务层只依赖 `KnowledgeVectorService`，后续更换 Qdrant、Milvus 等向量库时不影响 `KnowledgeBasesService`。

服务端依赖需要先确认：

```text
server/package.json
  -> dependencies.chromadb
```

如果依赖或 lock 文件缺失，先在 `server/` 下安装 Chroma JS 客户端：

```bash
npm.cmd install chromadb
```

注意：

- Chroma 客户端依赖只放在服务端 `server`，前端不直接连接向量库。
- Chroma 服务本身需要单独部署或本地启动，Node 依赖只是访问 Chroma 的客户端。
- 后端通过环境变量配置 Chroma 服务地址，不在业务代码中写死。

写入策略：

```text
collection: knowledge_chunks
id: chunk.vectorId
embedding: embedding 向量
document: 构造后的向量化文本
metadata: 来源信息
```

删除策略：

- 删除知识库时，删除该知识库全部向量。
- 删除文档时，删除该文档全部向量。
- 删除分片时，删除该分片向量。
- 分片内容更新时，用同一个 `vectorId` 覆盖写入。

### 16.7 检索链路调整

当前问答检索主要是数据库全文/关键词打分。接入向量后按检索配置分流：

| 检索模式 | 执行方式 |
| --- | --- |
| `fullText` | 只走数据库全文/关键词打分 |
| `vector` | 只走向量库召回 |
| `hybrid` | 数据库全文召回 + 向量召回，再融合排序 |

混合检索第一版排序建议：

```text
finalScore =
  textScore * textWeight
  + vectorScore * vectorWeight
  + matchPriority * 0.2
```

RRF 和 Rerank 可以先保留配置，不在第一版强制完成。

### 16.8 前端需要配合的最小改动

知识库列表：

- `索引` 按钮继续保留。
- `处理阶段` 显示 `indexing/indexed/failed`。
- `处理结果` 显示 `lastProcessMessage`。

分片内容 Tab：

- 增加分片级 `vectorStatus` 列。
- 增加 `vectorError` 悬浮或详情展示。
- 增加 `vectorizedAt` 展示。

手动分片弹窗：

- 新增/删除/改标题后无需立刻向量化，只把索引状态重置为待索引。
- 用户点击知识库列表的 `索引` 按钮后，再统一向量化。

### 16.9 明日建议执行顺序

1. 后端补 `KnowledgeBaseChunk` 向量字段和 DTO/前端类型。
2. 修正 `createChunk/updateChunk/removeChunk/replaceDocumentChunks/saveDocumentChunks` 的排序、字符数、hash 和索引状态重置。
3. 新增 `KnowledgeEmbeddingService`，封装调用 embedding 模型。
4. 确认服务端已安装 Chroma JS 客户端依赖 `chromadb`，如缺失则在 `server/` 执行 `npm.cmd install chromadb`。
5. 新增 `KnowledgeVectorService`，先定义接口和 Chroma 具体实现。
6. 补充 Chroma 环境变量示例，例如 `CHROMA_URL`、`CHROMA_COLLECTION`。
7. 改造 `indexBase/executeIndexBase`，真实执行向量化和向量库 upsert。
8. 删除知识库、删除文档、删除分片时同步删除向量。
9. 改造 `KnowledgeAiChatRetrievalService`，按检索模式接入向量召回。
10. 前端补分片向量状态展示和失败原因展示。
11. 用一条文本知识库和一条手动分片知识库做端到端测试。
12. 再测试 PDF/Word 解析后的自动分片索引。

### 16.10 明日执行验收标准

必须满足：

- 手动分片新增后 `chunkIndex/sort` 连续稳定。
- 删除中间分片后，剩余分片顺序重新连续。
- 改标题或内容后，知识库 `indexStatus` 回到 `pending`。
- 点击 `索引` 后，分片进入 `processing -> success` 或 `failed`。
- 向量库中能按 `vectorId` 找到对应分片。
- AI 问答在绑定检索配置后能命中向量召回内容。
- 失败原因能显示在知识库列表或分片详情中。

### 16.11 已落地实现记录

本阶段已完成第一版真实向量索引链路：

1. `knowledge_base_chunks` 已补充 `vectorId`、`contentHash`、`vectorStatus`、`vectorError`、`vectorizedAt`。
2. 新增 `KnowledgeEmbeddingService`，统一读取 `featureType = embedding` 且已启用的 AI 功能配置，调用所选大模型账号下的向量模型。
3. 新增 `KnowledgeVectorService`，第一版接入 Chroma，提供 `upsertChunks`、`deleteChunks`、`search`。
4. `POST /api/knowledge-bases/:id/index` 已从占位标记改为真实异步索引任务：计算 hash、跳过未变化分片、调用 embedding、写入 Chroma、回写分片状态。
5. 删除知识库、删除文档、删除分片、重新自动分片、手动覆盖分片时，会按已有 `vectorId` 删除旧向量。
6. 检索配置的 `fullText`、`vector`、`hybrid` 已接入服务端检索链路；`hybrid` 会融合全文/关键词分数与向量召回分数。
7. 知识库详情的分片内容 Tab 已展示分片级向量状态、失败原因和向量化时间。

运行前置：

```text
1. 后端需配置并启用 AI 功能配置：featureType = embedding。
2. 大模型账号需维护 embeddingModels，并保证所选模型支持 OpenAI 兼容 embeddings 接口。
3. Chroma 服务需单独启动，后端通过 CHROMA_URL / CHROMA_COLLECTION 连接。
```

### 16.12 本项目 Chroma 本地服务约定

当前项目中的 `server` 只负责业务逻辑和通过 Node 版 `chromadb` 客户端访问 Chroma。`chromadb` 不是数据库服务本体，因此需要额外启动一个独立的 Chroma 服务。

本地开发约定如下：

```text
chroma-server/       -> Python 版 Chroma 服务启动脚本与依赖
chroma-data/         -> Chroma 向量数据持久化目录，不提交仓库
server/package.json  -> 提供 chroma:install / chroma:start 辅助命令
```

首次使用时，在 `server` 目录执行：

```powershell
npm.cmd run chroma:install
```

启动本地 Chroma 服务：

```powershell
npm.cmd run chroma:start
```

默认服务地址为：

```text
http://localhost:8000
```

默认持久化目录为：

```text
项目根目录/chroma-data
```

也就是说，知识库分片向量化后的真实向量数据、文档副本和元数据会保存在 Chroma 服务管理的数据目录中；MySQL 只保存业务分片、向量状态、`vectorId`、`contentHash` 等业务追踪字段。

## 17. 推荐后续演进顺序

建议不要一次性把 Agent、向量库、OCR 全部混进当前服务。更稳的演进顺序：

1. 先保证文本、Word、PDF、图片能稳定解析出正文。
2. 保证自动分片和手动分片都能可靠保存。
3. 建立真实向量索引流程。
4. 在检索配置中真正接入向量检索和混合检索。
5. 在 AI 问答中展示命中来源和分片编号。
6. 再考虑 Agent 编排能力。

## 18. 简化时序图

```text
用户
  |
  | 新建知识库 / 上传文件
  v
knowledge_bases
  |
  | 解析任务 parse
  v
TaskQueueService
  |
  +-- manual -> DocumentParsersService -> StoredFilesService -> Parser
  |
  +-- mineru -> MineruConfigsService -> MinerU API
  |
  v
knowledge_base_documents.content
  |
  | 自动分片或手动分片
  v
knowledge_base_chunks
  |
  | 索引
  v
indexStatus = success
  |
  | 聊天应用绑定检索配置
  v
KnowledgeAiChatRetrievalService
  |
  | 生成参考资料上下文
  v
AI Chat
```
