# FullstackSeed 设计规范（Design Specification）

> 全局单一可信源（Single Source of Truth）。后续所有 UI 开发须先读取本文件。

## 1. 设计风格（Design Style）
- 美学：深色科技蓝（Dark Tech Blue），专业、克制、有科技感
- 核心特征：
  - 深色侧边栏 + 浅色内容区的经典中后台对比布局
  - 强调态以品牌蓝高亮，弱化非必要装饰
  - 平滑过渡（hover/active 0.2s），无生硬跳变

## 2. 配色（Color Palette）
- 侧边栏背景 Sidebar BG：`#304156`
- 侧边栏深一档（active 底色）：`#263445`
- 品牌蓝 Primary：`#409EFF`
- Hover 底色（品牌蓝半透明）：`rgba(64, 158, 255, 0.12)`
- Active 左侧高亮条：`#409EFF`
- 菜单常规文字：`#bfcbd9`
- 菜单 hover/active 文字：`#ffffff`
- 内容区背景 Content BG：`#f0f2f5`
- 顶栏背景 Header BG：`#ffffff`
- 分隔线 Border：`#dcdfe6`

## 3. 字体（Typography）
- 字体族：系统默认（继承 Element Plus）
- 菜单项：14px / 600（active 加粗）
- Logo：16px / 600

## 4. 图标（Iconography）
- 库：Element Plus Icons
- 风格：Filled / Regular 混用，跟随菜单语义

## 5. Logo
- 位置：侧边栏顶部，居中
- 缩放：展开显示 `FullstackSeed`，收起显示 `FS`

## 6. 操作按钮 / 权限 Tag 配色标准

> 全站涉及「动作」的按钮、Tag、指示器，一律按下表映射，保证跨页面视觉一致。
> 工具函数：`web/src/utils/permission.ts` 的 `getPermissionActionColor(code)`。

### 6.1 动作 → Element Plus 语义类型

| 动作后缀 | Element `type` | 视觉 | 语义 | 举例权限码 |
| --- | --- | --- | --- | --- |
| `read` / `view` | `info` | 灰蓝 | 无副作用查询 | `User.read` |
| `create` | `success` | 绿色 | 新增，正向操作 | `User.create` |
| `update` / `edit` | `primary` | 品牌蓝 | 修改，主操作 | `User.update` |
| `delete` | `danger` | 红色 | 破坏性 | `User.delete` |
| `batchDelete` | `danger` | 红色（同 delete） | 更强破坏 | `User.batchDelete` |
| `export` / `import` / `audit` | `warning` | 橙色 | 需注意 | `Order.audit` |
| 其它未识别 | `info` | 灰蓝 | 兜底 | — |

### 6.2 通用规则
- **按动作后缀判色**（`.` 之后的部分），不区分模块
- Tag 与 Button 共用同一 `type` 映射，同一动作在列表 tag 与操作按钮里颜色一致
- Tag 一律采用 `light`（默认）effect，不使用 `dark`/`plain`
- 未识别的动作 → `info` 灰色兜底，禁止随意自选颜色

### 6.3 强制使用点
- `ProTable` 内置操作列的「查看/编辑/删除」按钮：`info` / `primary` / `danger`
- 权限管理页操作权限列 Tag：调 `getPermissionActionColor(code)`
- 业务页新增自定义操作按钮或 Tag：必须走同一映射，不得内联硬编码 `type="warning"` 等

### 6.4 短标签（可选）
`getPermissionActionLabel(code)` 提供中文短标签映射：`read→查看`、`create→新增`、`update→编辑`、`delete→删除`、`batchDelete→批量删除`、`export→导出`、`import→导入`、`audit→审核`。

## 维护规则
- 一致性：所有 UI 开发须先读取本文件
- 同步：任何设计变更须同时更新本文件
